node:
  cluster: cluster
  id: backend

dynamic_resources:
  lds_config:
    path: /var/lib/envoy/lds.yaml
  cds_config:
    path: /var/lib/envoy/cds.yaml

admin:
  address:
    socket_address:
      protocol: TCP
      address: 0.0.0.0
      port_value: 9901
